package listeners

// {{.Name}} handles an event. Register it with a typed listener:
//
//	events.Listen(dispatcher, {{.Name}})
func {{.Name}}(event *YourEvent) error {
	// Handle the event here.
	return nil
}
