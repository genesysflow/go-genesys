package tests

import (
	"testing"
)

// Test{{.Name}} exercises the behaviour it names.
func Test{{.Name}}(t *testing.T) {
	// Arrange, act, assert. For HTTP:
	//
	//	tc := http.NewTestCase(t, kernel)
	//	tc.Get("/users").AssertOK().AssertJsonCount("data", 3)
}
