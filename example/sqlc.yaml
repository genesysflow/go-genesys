version: "2"
sql:
  - engine: "sqlite"
    queries: "database/queries/"
    schema: "database/schema/"
    gen:
      go:
        package: "db"
        out: "database/db"
        emit_json_tags: true
        emit_prepared_queries: true
        emit_interface: true
        emit_empty_slices: true
