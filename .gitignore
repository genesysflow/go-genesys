# Local environment: never committed, since it holds this machine's keys
# and credentials. Copy .env.example and fill it in.
.env

# The example's local sqlite database and generated files.
example/storage/database.sqlite-shm
example/storage/database.sqlite-wal
